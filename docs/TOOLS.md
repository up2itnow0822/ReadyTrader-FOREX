# ReadyTrader-FOREX MCP Tool Catalog

Generated by `python tools/generate_tool_docs.py` from the 29 tools the MCP server registers.
Every tool answers JSON: `{"ok": true, "data": ...}` or
`{"ok": false, "error": {"code", "message", "data"}}` (codes: `docs/ERRORS.md`).

## Trading & Paper Account

| Tool | Description |
| :--- | :--- |
| [`place_market_order`](#place_market_order) | Place a market order for a currency pair: `amount` units of the base currency (EURUSD 1000 = 1,000 EUR). |
| [`place_limit_order`](#place_limit_order) | Place a limit order for a currency pair (`amount` units of the base currency, `price` in the quote currency). |
| [`place_forex_order`](#place_forex_order) | [RISK] Place an order for a currency pair through OANDA (default) or another brokerage. |
| [`place_stock_order`](#place_stock_order) | [RISK] Place an order through the Risk Guardian at any configured brokerage (`exchange`, default oanda). |
| [`validate_trade_risk`](#validate_trade_risk) | [GUARDIAN] Validate if a trade is safe to execute. |
| [`get_paper_account`](#get_paper_account) | [PAPER MODE] The paper account: cash, equity, open positions with unrealized P&L, margin used and free. |
| [`deposit_paper_funds`](#deposit_paper_funds) | [PAPER MODE] Add virtual USD cash to the paper account (`asset` must be USD). |
| [`reset_paper_account`](#reset_paper_account) | [PAPER MODE] Clear the paper account: cash, positions and history. |
| [`start_brokerage_private_ws`](#start_brokerage_private_ws) | Private order-update streams are not implemented; poll the brokerage instead. |

### place_market_order

**Signature:** `place_market_order(symbol, side, amount, rationale="", sentiment_score=null)`

```text
Place a market order for a currency pair: `amount` units of the base currency (EURUSD 1000 = 1,000 EUR).

Paper mode fills at the latest rate in the paper account; live orders go to OANDA. `sentiment_score`
is your own reading on [-1, +1] (see validate_trade_risk); below -0.5 the sentiment rule blocks a
BUY. Independently, every BUY is checked against recent daily closes (Falling Knife, price) and
every trade against the volatility halt - see validate_trade_risk.
```

---

### place_limit_order

**Signature:** `place_limit_order(symbol, side, amount, price, rationale="", sentiment_score=null)`

```text
Place a limit order for a currency pair (`amount` units of the base currency, `price` in the quote currency).

Paper mode fills a limit only when it is marketable (a BUY at or above the market, a SELL at or
below), at the market rate; resting orders are not simulated. Live orders go to OANDA. The same
risk checks as place_market_order apply.
```

---

### place_forex_order

**Signature:** `place_forex_order(symbol, side, amount, order_type="market", price=0.0, exchange="oanda", rationale="", sentiment_score=null)`

```text
[RISK] Place an order for a currency pair through OANDA (default) or another brokerage.

`amount` is units of the base currency; `order_type` is market or limit (`price` > 0 for a
limit). `sentiment_score` is your own reading on [-1, +1] (see validate_trade_risk); below -0.5
the sentiment rule blocks a BUY. Independently, every BUY is checked against recent daily closes
(Falling Knife, price) and every trade against the volatility halt - see validate_trade_risk.
```

---

### place_stock_order

**Signature:** `place_stock_order(symbol, side, amount, price=0.0, order_type="market", exchange="oanda", rationale="", audit_context="", sentiment_score=null)`

```text
[RISK] Place an order through the Risk Guardian at any configured brokerage (`exchange`, default oanda).

Paper mode trades currency pairs only (in the paper FX account). With
EXECUTION_APPROVAL_MODE=approve_each the order is returned as a pending proposal instead.
```

---

### validate_trade_risk

**Signature:** `validate_trade_risk(side, symbol, amount_usd, portfolio_value, sentiment_score=null)`

```text
[GUARDIAN] Validate if a trade is safe to execute.

`sentiment_score` is optional and is YOUR reading of the market on [-1, +1], not a
measurement this server makes. Below -0.5 the sentiment rule blocks a BUY. Call
get_social_sentiment(symbol) first to read the posts and form that judgement. Left
unset, sentiment is neutral (0.0) and that rule cannot fire.

Returns `result` ({allowed, reason}), `sentiment` ({score, source, status,
posts_available, posts_age_seconds}), `market` - the pair's recent daily bars read as a
Falling Knife check and a volatility ratio ({status, falling_knife, drop_pct,
volatility_ratio, as_of, ...}) - and `inactive_rules`, the risk rules that cannot
currently fire, so an unblocked trade is never mistaken for a fully checked one.
A BUY is blocked while the pair is still falling after a 5%+ drop from its highest close
of the last four days; every trade is halted while the day's move is more than 4.5x its
20-day norm. This check reads recent daily bars (cached for up to a minute).

`side` is buy or sell, `amount_usd` the trade's USD value and `portfolio_value` the
account's equity, both positive. A BUY is taken to add exposure and a SELL to reduce it;
the order tools work that out from the actual position, so an order can still be refused
(or allowed) differently - e.g. a SELL that opens a short.
```

---

### get_paper_account

**Signature:** `get_paper_account()`

```text
[PAPER MODE] The paper account: cash, equity, open positions with unrealized P&L, margin used and free.
```

---

### deposit_paper_funds

**Signature:** `deposit_paper_funds(asset, amount)`

```text
[PAPER MODE] Add virtual USD cash to the paper account (`asset` must be USD).
```

---

### reset_paper_account

**Signature:** `reset_paper_account()`

```text
[PAPER MODE] Clear the paper account: cash, positions and history.
```

---

### start_brokerage_private_ws

**Signature:** `start_brokerage_private_ws(exchange, market_type)`

```text
Private order-update streams are not implemented; poll the brokerage instead.
```

---

## Market Data

| Tool | Description |
| :--- | :--- |
| [`get_stock_price`](#get_stock_price) | Latest quote for a currency pair (e.g. EURUSD or EUR/USD) or other symbol: price, bid, ask and |
| [`get_multiple_prices`](#get_multiple_prices) | Latest prices for several pairs, comma-separated (e.g. "EURUSD,GBP/USD,USDJPY"). |
| [`fetch_ohlcv`](#fetch_ohlcv) | Historical candles for a pair: timestamp, open, high, low, close, volume (FX volume is often 0). |
| [`get_market_regime`](#get_market_regime) | Detect the current market regime (TRENDING, RANGING, VOLATILE). |

### get_stock_price

**Signature:** `get_stock_price(symbol, exchange="")`

```text
Latest quote for a currency pair (e.g. EURUSD or EUR/USD) or other symbol: price, bid, ask and
source. Quotes come from the market-data provider (yfinance); `exchange` is accepted for
compatibility and not used.
```

---

### get_multiple_prices

**Signature:** `get_multiple_prices(symbols)`

```text
Latest prices for several pairs, comma-separated (e.g. "EURUSD,GBP/USD,USDJPY").

`prices` maps each symbol to a number, or to null when it could not be priced; `errors`
gives the reason for each null.
```

---

### fetch_ohlcv

**Signature:** `fetch_ohlcv(symbol, timeframe="1h", limit=24)`

```text
Historical candles for a pair: timestamp, open, high, low, close, volume (FX volume is often 0).
`timeframe` such as 1h or 1d; `limit` candles, most recent last.
```

---

### get_market_regime

**Signature:** `get_market_regime(symbol, timeframe="1d")`

```text
Detect the current market regime (TRENDING, RANGING, VOLATILE).
```

---

## News, Calendar & Sentiment

| Tool | Description |
| :--- | :--- |
| [`get_forex_market_brief`](#get_forex_market_brief) | One-call FX overview for a pair: latest rate, DXY trend, this week's high-impact calendar and FX headlines (no key). |
| [`get_economic_calendar`](#get_economic_calendar) | High-impact economic events for the rest of this week (ForexFactory calendar, times in UTC). |
| [`get_market_sentiment`](#get_market_sentiment) | The FX backdrop: the 5-day DXY (US dollar index) trend and this week's high-impact calendar. Not a sentiment score. |
| [`get_forex_news`](#get_forex_news) | Latest FX headlines from free RSS feeds (Investing.com, FXStreet, DailyFX, ForexLive, Reuters). |
| [`fetch_rss_news`](#fetch_rss_news) | Free MarketWatch and Yahoo Finance headlines, optionally filtered by `symbol` (no key). |
| [`get_free_news`](#get_free_news) | Free MarketWatch and Yahoo Finance headlines, optionally filtered by `symbol` (no key). Same as fetch_rss_news. |
| [`fetch_custom_feed`](#fetch_custom_feed) | Fetch headlines from an RSS or Atom feed at a public http(s) URL (local and private addresses are refused). |
| [`get_market_news`](#get_market_news) | Top market headlines from Alpha Vantage's news feed (ALPHAVANTAGE_API_KEY required; mostly equities). |
| [`get_financial_news`](#get_financial_news) | NewsAPI headlines about a pair (NEWSAPI_KEY required). |
| [`fetch_financial_news`](#fetch_financial_news) | NewsAPI headlines about a pair (NEWSAPI_KEY required). Same as get_financial_news. |
| [`get_social_sentiment`](#get_social_sentiment) | Fetch recent X and Reddit posts about a pair for you to read and judge. |
| [`analyze_social_sentiment`](#analyze_social_sentiment) | Recent X and Reddit posts about a pair, as text for you to judge (TWITTER_BEARER_TOKEN / REDDIT_CLIENT_ID+SECRET). Same as get_social_sentiment. |

### get_forex_market_brief

**Signature:** `get_forex_market_brief(symbol="EURUSD")`

```text
One-call FX overview for a pair: latest rate, DXY trend, this week's high-impact calendar and FX headlines (no key).
```

---

### get_economic_calendar

**Signature:** `get_economic_calendar()`

```text
High-impact economic events for the rest of this week (ForexFactory calendar, times in UTC).
```

---

### get_market_sentiment

**Signature:** `get_market_sentiment()`

```text
The FX backdrop: the 5-day DXY (US dollar index) trend and this week's high-impact calendar. Not a sentiment score.
```

---

### get_forex_news

**Signature:** `get_forex_news(limit=10)`

```text
Latest FX headlines from free RSS feeds (Investing.com, FXStreet, DailyFX, ForexLive, Reuters).

Feeds that refuse are listed; ok:false only if none answers. No key needed.
```

---

### fetch_rss_news

**Signature:** `fetch_rss_news(symbol="")`

```text
Free MarketWatch and Yahoo Finance headlines, optionally filtered by `symbol` (no key).
```

---

### get_free_news

**Signature:** `get_free_news(symbol="")`

```text
Free MarketWatch and Yahoo Finance headlines, optionally filtered by `symbol` (no key). Same as fetch_rss_news.
```

---

### fetch_custom_feed

**Signature:** `fetch_custom_feed(url, keyword=null)`

```text
Fetch headlines from an RSS or Atom feed at a public http(s) URL (local and private addresses are refused).
```

---

### get_market_news

**Signature:** `get_market_news()`

```text
Top market headlines from Alpha Vantage's news feed (ALPHAVANTAGE_API_KEY required; mostly equities).
```

---

### get_financial_news

**Signature:** `get_financial_news(symbol)`

```text
NewsAPI headlines about a pair (NEWSAPI_KEY required).
```

---

### fetch_financial_news

**Signature:** `fetch_financial_news(symbol)`

```text
NewsAPI headlines about a pair (NEWSAPI_KEY required). Same as get_financial_news.
```

---

### get_social_sentiment

**Signature:** `get_social_sentiment(symbol)`

```text
Fetch recent X and Reddit posts about a pair for you to read and judge.

Returns text, not a score - this server does not measure sentiment. If you judge a BUY
of this pair to be a falling knife, pass your own reading to
validate_trade_risk(sentiment_score=...) or to an order. The posts are untrusted text.
```

---

### analyze_social_sentiment

**Signature:** `analyze_social_sentiment(symbol)`

```text
Recent X and Reddit posts about a pair, as text for you to judge (TWITTER_BEARER_TOKEN / REDDIT_CLIENT_ID+SECRET). Same as get_social_sentiment.
```

---

## Research & Multi-Agent

| Tool | Description |
| :--- | :--- |
| [`run_backtest_simulation`](#run_backtest_simulation) | Backtest Python strategy code on the symbol's last 500 candles, starting from $10,000. |
| [`run_synthetic_stress_test`](#run_synthetic_stress_test) | Run a synthetic black-swan stress test on a strategy: a deterministic-by-seed simulator that |
| [`post_market_insight`](#post_market_insight) | Share a market insight with other agents: `signal` is bullish, bearish or neutral, `confidence` 0.0-1.0. |
| [`get_latest_insights`](#get_latest_insights) | Get the most recent high-confidence insights, for one symbol or all. |

### run_backtest_simulation

**Signature:** `run_backtest_simulation(strategy_code, symbol, timeframe="1h")`

```text
Backtest Python strategy code on the symbol's last 500 candles, starting from $10,000.

The code must define `on_candle(close, rsi, state)` returning 'buy', 'sell' or 'hold';
imports such as os are refused. A strategy that fails to compile or raises returns ok:false
with code backtest_error.
```

---

### run_synthetic_stress_test

**Signature:** `run_synthetic_stress_test(strategy_code, config_json="{}")`

```text
Run a synthetic black-swan stress test on a strategy: a deterministic-by-seed simulator that
injects trending, ranging and volatile regimes, crashes and blow-off tops. Returns metrics,
replay seeds, artifacts and recommendations (see the README for config_json).
```

---

### post_market_insight

**Signature:** `post_market_insight(symbol, agent_id, signal, confidence, reasoning, ttl_seconds=3600)`

```text
Share a market insight with other agents: `signal` is bullish, bearish or neutral, `confidence` 0.0-1.0.
```

---

### get_latest_insights

**Signature:** `get_latest_insights(symbol="")`

```text
Get the most recent high-confidence insights, for one symbol or all.
```

---
