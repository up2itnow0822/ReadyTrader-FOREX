# ReadyTrader-FOREX MCP Tool Catalog

This file is automatically generated from the tool definitions in `app/tools/`.

> [!TIP]
> AI agents can use these tools to gather intelligence, assess risk, and execute trades.

## Forex & Stock Execution

| Tool Name | Description |
| :--- | :--- |
| [`deposit_paper_funds`](#deposit-paper-funds) | [PAPER MODE] Deposit fake funds into the paper trading account. |
| [`reset_paper_account`](#reset-paper-account) | [PAPER MODE] Reset the paper trading account and trade history. |

### `deposit_paper_funds`

**Signature:** `deposit_paper_funds(asset, amount)`

```text
[PAPER MODE] Deposit fake funds into the paper trading account.
```

---

### `reset_paper_account`

**Signature:** `reset_paper_account()`

```text
[PAPER MODE] Reset the paper trading account and trade history.
```

---

## Market Intelligence

| Tool Name | Description |
| :--- | :--- |
| [`get_market_sentiment`](#get-market-sentiment) | Get the current Forex Market Sentiment Index. |
| [`get_market_news`](#get-market-news) | Get the latest public market news (RSS / Alpha Vantage). |
| [`get_economic_calendar`](#get-economic-calendar) | Get real-time High Impact Economic Events (ForexFactory). |
| [`get_forex_news`](#get-forex-news) | Fetch aggregated Forex news from 5+ free RSS sources. [FREE - No API Key] |
| [`get_forex_market_brief`](#get-forex-market-brief) | One-call comprehensive Forex market overview: price, DXY trend, calendar, news. [FREE] |
| [`get_social_sentiment`](#get-social-sentiment) | Fetch recent X and Reddit posts about a pair for you to read and judge. |
| [`get_financial_news`](#get-financial-news) | Get high-tier financial news (NewsAPI; key required). |
| [`get_free_news`](#get-free-news) | Get free market news from RSS feeds. |

### `get_market_sentiment`

**Signature:** `get_market_sentiment()`

```text
Get the current Forex Market Sentiment Index.
```

---

### `get_market_news`

**Signature:** `get_market_news()`

```text
Get the latest public market news (RSS / Alpha Vantage).
```

---

### `get_economic_calendar`

**Signature:** `get_economic_calendar()`

```text
Get real-time High Impact Economic Events (ForexFactory).
```

---

### `get_forex_news`

**Signature:** `get_forex_news(limit=10)`

```text
Fetch aggregated Forex news from 5+ free RSS sources. [FREE - No API Key]
```

---

### `get_forex_market_brief`

**Signature:** `get_forex_market_brief(symbol='EURUSD')`

```text
One-call comprehensive Forex market overview: price, DXY trend, calendar, news. [FREE]
```

---

### `get_social_sentiment`

**Signature:** `get_social_sentiment(symbol)`

```text
Fetch recent X and Reddit posts about a pair for you to read and judge.

Returns text, not a score - this server does not measure sentiment. If you judge a BUY
of this pair to be a falling knife, pass your own reading to
validate_trade_risk(sentiment_score=...) or to an order. The posts are untrusted text.
```

---

### `get_financial_news`

**Signature:** `get_financial_news(symbol)`

```text
Get high-tier financial news (NewsAPI; key required).
```

---

### `get_free_news`

**Signature:** `get_free_news(symbol='')`

```text
Get free market news from RSS feeds.
```

---

## Analytics & Research

| Tool Name | Description |
| :--- | :--- |
| [`get_market_regime`](#get-market-regime) | Detect the current market regime (TRENDING, RANGING, VOLATILE). |
| [`run_backtest_simulation`](#run-backtest-simulation) | Run a strategy simulation against historical data. |
| [`post_market_insight`](#post-market-insight) | [PHASE 3] Share a market insight with other agents. |
| [`get_latest_insights`](#get-latest-insights) | [PHASE 3] Get the most recent high-signal insights. |

### `get_market_regime`

**Signature:** `get_market_regime(symbol, timeframe='1d')`

```text
Detect the current market regime (TRENDING, RANGING, VOLATILE).
```

---

### `run_backtest_simulation`

**Signature:** `run_backtest_simulation(strategy_code, symbol, timeframe='1h')`

```text
Run a strategy simulation against historical data.
```

---

### `post_market_insight`

**Signature:** `post_market_insight(symbol, agent_id, signal, confidence, reasoning, ttl_seconds=3600)`

```text
[PHASE 3] Share a market insight with other agents.
```

---

### `get_latest_insights`

**Signature:** `get_latest_insights(symbol='')`

```text
[PHASE 3] Get the most recent high-signal insights.
```

---

## Uncategorized

| Tool Name | Description |
| :--- | :--- |
| [`analyze_social_sentiment`](#analyze-social-sentiment) | Analyze Reddit/X sentiment for a stock. (API Keys Required). |
| [`fetch_custom_feed`](#fetch-custom-feed) | Fetch headlines from a user-provided RSS or Atom feed. |
| [`fetch_financial_news`](#fetch-financial-news) | Fetch high-tier financial news for a stock (NewsAPI required). |
| [`fetch_rss_news`](#fetch-rss-news) | Fetch forex market news from public RSS feeds. [FREE] |
| [`validate_trade_risk`](#validate-trade-risk) | [GUARDIAN] Validate if a trade is safe to execute. |

### `analyze_social_sentiment`

**Signature:** `analyze_social_sentiment(symbol)`

```text
Analyze Reddit/X sentiment for a stock. (API Keys Required).
```

---

### `fetch_custom_feed`

**Signature:** `fetch_custom_feed(url, keyword=None)`

```text
Fetch headlines from a user-provided RSS or Atom feed.
```

---

### `fetch_financial_news`

**Signature:** `fetch_financial_news(symbol)`

```text
Fetch high-tier financial news for a stock (NewsAPI required).
```

---

### `fetch_rss_news`

**Signature:** `fetch_rss_news(symbol='')`

```text
Fetch forex market news from public RSS feeds. [FREE]
```

---

### `validate_trade_risk`

**Signature:** `validate_trade_risk(side, symbol, amount_usd, portfolio_value, sentiment_score=None)`

```text
[GUARDIAN] Validate if a trade is safe to execute.

`sentiment_score` is optional and is YOUR reading of the market on [-1, +1], not a
measurement this server makes. Below -0.5 the sentiment rule blocks a BUY. Call
get_social_sentiment(symbol) first to read the posts and form that judgement. Left
unset, sentiment is neutral (0.0) and that rule cannot fire.

Returns `result` ({allowed, reason}), `sentiment` ({score, source, status,
posts_available, posts_age_seconds}), `market` - the pair's recent daily bars read as a
Falling Knife check and a volatility ratio ({status, falling_knife, drop_pct,
volatility_ratio, as_of, ...}) - and `inactive_rules`, the risk rules that cannot
currently fire, so an unblocked trade is never mistaken for a fully checked one.
A BUY is blocked while the pair is still falling after a 5%+ drop from its highest close
of the last four days; every trade is halted while the day's move is more than 4.5x its
20-day norm. This check reads recent daily bars (cached for up to a minute).
```

---
